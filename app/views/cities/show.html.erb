<h1><%= @city.site_name  %></h1>
<p><%= @city.tag_line %></p>

<% @city.shows.each do | show | %>
  <p><%= show.start_date  %></p>
  <p> 
  <% show.bands.each do |band| %>
    <a target="_blank" href="https://<%= band.bandcamp %>.bandcamp.com"><%= band.name %></a> 
    <%  unless show.bands.last == band %> // <% end %> 
      
  <% end %>
  -<%= show.start_time %> @ <%= show.venue  %>
  </p>
 

<% end %>
